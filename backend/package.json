{
  "name": "recipes-backend",
  "version": "1.0.0",
  "description": "a tiny server i created",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "db:up": "docker compose -f ../docker-compose.yml up -d mongo",
    "db:down": "docker compose -f ../docker-compose.yml stop mongo",
    "dev": "npm run db:up && nodemon src/server.js",
    "start": "node src/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.18.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
